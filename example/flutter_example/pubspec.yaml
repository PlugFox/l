name: l_flutter_example
description: Flutter [L]ogger example

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  l:
    path: '../../'

flutter:
  uses-material-design: true
